# storefront-demo

Minimal static demo (**Milestone 0**): Vite-less single HTML builder that calls `POST /v1/search` via the gateway.

**Milestone 1** extends this with CSV-backed catalog and real hybrid results.
