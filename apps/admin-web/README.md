# admin-web

Next.js merchant console lands in **Milestone 2** (feed mapping wizard, relevance tuning, playground).

This package is a **placeholder** until `create-next-app` is wired into the Turborepo pipeline.
