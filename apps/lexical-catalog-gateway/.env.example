# Path to baked catalog (same shape as apps/showcase/lib/demo-catalog.json).
# Build locally first: SHOWCASE_DEMO_ROW_LIMIT=0 npm run showcase:demo-catalog — from repo root.
# LEXICAL_GATEWAY_CATALOG_JSON=../../apps/showcase/lib/demo-catalog.json

PORT=3040
# LEXICAL_GATEWAY_API_KEY=pk_demo
