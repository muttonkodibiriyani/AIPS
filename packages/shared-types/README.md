# @commerce-ai/shared-types

Types generated from [`contracts/openapi.yaml`](../../contracts/openapi.yaml).

From repository root (after `pnpm install`):

```bash
pnpm types:generate
```

This overwrites [`src/generated/api.ts`](src/generated/api.ts).
